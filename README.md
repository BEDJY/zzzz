# Dashboard Analítico Pro

Tablero de control administrativo con gráficos e informes en vivo de ventas e ingresos.

## Estructura SQL D1:
- Tabla `sales`: Registra transacciones con campos `id`, `client`, `amount` y `date`.
